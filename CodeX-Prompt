你是 Codex GPT-5.4。请直接在当前工作区实现一个可运行的 Windows 11 系统托盘语音输入应用，不要只给方案，不要留关键 TODO，不要用伪代码糊弄。优先保证“真的能跑、真的能输入文字、真的能处理中文”，其次再追求代码美观。遇到 Windows 用户态做不到 100% 可靠的点，必须做可运行降级，并在 README 中明确写清楚，不要假装已经完美实现。

目标产品：一个常驻系统托盘的 Windows 语音输入法应用。按住热键开始录音，松开后将识别出的文字注入当前聚焦输入框。默认识别简体中文，并支持菜单切换语言。录音时屏幕底部中央出现优雅精致的胶囊悬浮窗，左侧是实时音量驱动的 5 柱波形，右侧实时显示 partial transcript。松开热键后，如果启用了 LLM refinement，则先做极保守纠错，再注入文本。

一、技术栈与项目约束
1. 使用 C# 12 + .NET 8 + WPF，目标系统为 Windows 11 23H2+。
2. 项目必须是原生 Windows 桌面应用，不要用 Electron、Tauri、MAUI、WebView 套壳。
3. 使用系统托盘常驻模式启动，不显示主窗口，不在任务栏出现，不在 Alt+Tab 中出现。
4. 使用单实例模式；重复启动时应唤醒已有实例而不是开第二个进程。
5. 建议使用这些库，但不要过度依赖花哨框架：
   - `NAudio`：麦克风采集和实时 RMS
   - `H.NotifyIcon.Wpf`：系统托盘
   - `CommunityToolkit.Mvvm`：MVVM
   - `Whisper.net` 或可行的 `whisper.cpp` .NET 封装：本地多语言语音识别
6. 所有关键功能都要有真实实现：全局热键、录音、partial transcript、悬浮窗动画、剪贴板注入、IME 切换、LLM refine、设置持久化。
7. 代码结构清晰，拆成服务层，不要把所有逻辑塞进 App.xaml.cs 或一个巨大 Window 里。

二、按键与录音行为
1. 产品形态上优先支持“按住 Fn 说话，松开 Fn 注入文本”。
2. 但必须正视 Windows 的现实：多数机器上的 Fn 是固件级按键，用户态应用通常无法通过标准低级键盘钩子稳定收到它。
3. 因此必须这样设计：
   - 实现一个 `IPushToTalkTrigger` 抽象。
   - 优先尝试 best-effort 的 Fn 检测（如 Raw Input / vendor scancode / 低级钩子可观察路径）。
   - 如果当前机器无法可靠检测 Fn，则自动降级到一个可配置、可全局捕获且可抑制传递的热键，默认使用 `Right Ctrl`。
   - 托盘菜单或设置界面中必须明确显示当前实际生效的 PTT 键位。
4. 按下并保持 PTT 时：
   - 开始录音
   - 显示悬浮窗
   - 开始实时 partial transcript
5. 松开 PTT 时：
   - 停止录音
   - 完成最终转录
   - 如果启用 LLM refinement 且配置完整，先显示 `Refining...` 并等待结果
   - 最终把文本注入当前焦点输入框
6. 对于实际生效的 PTT 键，尽量抑制其继续传递给前台应用，避免触发宿主应用快捷键或输入异常。
7. 不允许用“按一下开始、再按一下结束”的切换式录音；必须是按住说话、松开完成。

三、语音识别与语言
1. 默认语言必须是简体中文，开箱即用优先识别中文。
2. 菜单中提供语言切换：
   - English
   - 简体中文
   - 繁体中文
   - 日本語
   - 한국어
3. 当前语言选择要持久化保存，应用重启后恢复。
4. 语音识别后端优先使用本地多语言 Whisper 路线，原因：
   - 不依赖云端即可工作
   - 对中英混杂更稳
   - 更适合做“开箱即用”的桌面工具
5. 不要把大型模型文件直接硬编码进仓库；请实现一个真实可用的模型管理策略：
   - 首选：首次启动时自动下载默认多语言模型到 `%LocalAppData%\<AppName>\Models`
   - 或者：提供 `scripts/download-model.ps1` + 应用内缺失提示与一键触发
   - 总之不要要求用户手工查文档自己折腾
6. 识别过程中必须提供 partial transcript，而不是等到松键后一次性出整段结果。
7. 即使底层实现是分块增量解码，也要让用户看到持续更新的文本。
8. 对中英混杂场景要特别注意：
   - 默认语言虽然是 `zh`
   - 但不要把明显英文技术词全部误转成中文音译
   - partial / final transcript 的文本拼接要稳定，不要来回抖动得很难看

四、悬浮窗 UI（非常重要，不要做丑）
1. 录音时在屏幕底部居中显示一个无边框、不可激活、始终置顶、胶囊状悬浮窗。
2. 使用 WPF 自定义无边框窗口实现，要求：
   - 不显示标题栏
   - 不显示红绿灯/系统按钮
   - 不出现在任务栏
   - 不出现在 Alt+Tab
   - 尽量不抢焦点（no-activate）
3. 视觉风格要精致，类似高质量系统 HUD：
   - 高度固定 56px
   - 圆角半径 28px
   - 胶囊左右内边距舒适
   - 使用半透明毛玻璃 / acrylic / mica 风格中的可行方案
   - 叠加轻微描边和柔和阴影
4. 左侧是 5 根竖条波形区域，整体尺寸 44x32px。
5. 这 5 根竖条必须由实时音频 RMS 电平驱动，绝对不要用写死的假动画。
6. 柱条权重固定为：
   - `[0.5, 0.8, 1.0, 0.75, 0.55]`
   形成中间高、两侧低的自然结构。
7. 包络平滑：
   - attack 40%
   - release 15%
8. 每根柱条叠加 `±4%` 的轻微随机抖动，增加有机感，但不要让波形看起来乱跳。
9. 波形幅度必须足够大、清晰可见；用户说话声音大时，波形明显变高；安静时明显回落。
10. 右侧是文本标签：
   - 实时显示 partial transcript
   - 无内容时显示 `Listening...`
   - LLM refine 阶段显示 `Refining...`
11. 文本区域宽度是弹性的：
   - 最小 160px
   - 最大 560px
   - 随文本增多平滑变宽
12. 动画要求：
   - 入场弹簧动画：0.35s
   - 文本宽度平滑过渡：0.25s
   - 退场缩放淡出：0.22s
13. 悬浮窗位置要考虑多显示器与任务栏；应显示在当前主显示器底部中央，离屏幕底边保留舒适安全间距。

五、音频采集与波形计算
1. 使用系统默认麦克风进行录音。
2. 采集链路要同时服务于：
   - 实时 RMS 计算
   - partial transcript
   - final transcript
3. RMS 计算要稳定，避免 UI 抖动过度。
4. 建议做法：
   - 以短帧窗口计算 RMS
   - 做归一化
   - 通过 attack/release 包络平滑
   - 再映射到 5 根柱条高度
5. 要处理静音、设备切换、麦克风不可用、权限被拒绝等情况，并给出清楚的托盘提示或状态文本。

六、文本注入、剪贴板与输入法切换
1. 最终文本注入必须使用“备份剪贴板 -> 写入文本 -> 模拟 `Ctrl+V` -> 恢复原剪贴板”的方式实现。
2. 不要尝试用逐字符键盘模拟输入整段文字；那样对中文和复杂应用不稳。
3. 注入前必须检测当前前台窗口的输入法/键盘布局。
4. 如果当前是 CJK 输入法或 CJK 键盘布局（中文/日文/韩文相关），则先临时切换到 ASCII 输入源 / 英文 US 键盘布局（如 `00000409`），再执行粘贴，粘贴后恢复原输入法/布局。
5. 这么做的目的是防止前台中文输入法拦截或污染 `Ctrl+V`。
6. 必须真实实现这个逻辑，不要只写注释。
7. 实现建议：
   - 获取前台窗口句柄
   - 获取其线程输入语言
   - 判断 LANGID / HKL 是否属于 CJK
   - 如有需要，发送 `WM_INPUTLANGCHANGEREQUEST` 或等价方案临时切到英文布局
   - 使用 `SendInput` 发送 `Ctrl+V`
   - 完成后恢复原 HKL
8. 剪贴板恢复要尽量完整：
   - 至少保证文本恢复可靠
   - 最好保留常见格式（Text / UnicodeText / Html / Rtf / Bitmap / FileDrop）
9. 如果剪贴板正被占用，做短暂重试；不要因为一次占用就直接失败。
10. 如果注入失败，至少保留结果在剪贴板并通过托盘通知告知用户。

七、LLM Refinement
1. 在基础语音识别完成后，增加一个可选的 LLM refinement 步骤，用于“极其保守地修复明显 ASR 错误”，尤其是中英混杂与技术术语场景。
2. 通过 OpenAI 兼容 API 实现，配置项包括：
   - API Base URL
   - API Key
   - Model
3. 在托盘菜单中提供 `LLM Refinement` 子菜单，包含：
   - 启用/禁用开关
   - `Settings...` 入口
4. 松开 PTT 后，如果 LLM refinement 已启用且配置完整，则：
   - 悬浮窗保持显示
   - 状态改为 `Refining...`
   - 调用 LLM
   - 用 refine 后结果作为最终注入文本
5. 如果 LLM 调用失败、超时或返回异常：
   - 自动回退到原始 transcript
   - 不要让整个输入流程卡死
6. API Key 输入框必须支持完全清空，保存空字符串后视为未配置，不允许出现“看起来清空了但实际上控件保留旧值”的问题。
7. `Test` 按钮必须真的发起一次测试请求，并把成功/失败明确反馈给用户。
8. `Save` 按钮必须原子化持久化配置。
9. 建议默认超时 8 秒左右，避免用户等待过久。
10. LLM 请求温度应接近 0，偏向稳定保守。

八、LLM system prompt（请直接内置，不要弱化）
把下面这段作为 refine 的系统指令核心语义，允许你做细微格式整理，但不要改变约束本身：

“你是一个极其保守的语音转写纠错器。你的唯一任务是修复明显的 ASR/语音识别错误。只允许修复非常明显的错误，例如：
- 中文同音/近音导致的错字
- 明显被错误音译成中文的英文技术术语，例如‘配森’应为‘Python’，‘杰森’应为‘JSON’
- 明显缺失或错误的标点（仅在非常确定时）

绝对不要：
- 改写句子
- 润色措辞
- 删除看起来正确的内容
- 补充原文中没有说出的信息
- 翻译内容
- 改变说话人的语气和风格
- 擅自压缩或扩写文本

如果输入看起来已经正确，必须原样逐字返回。
输出只能是最终文本本身，不要解释，不要加引号，不要加 Markdown，不要加前后缀。”

九、设置与持久化
1. 使用一个明确的设置服务，保存到 `%LocalAppData%\<AppName>\settings.json`。
2. 至少持久化这些字段：
   - 当前语言
   - 是否启用 LLM refinement
   - API Base URL
   - API Key
   - Model
   - 当前 PTT 键模式（Fn experimental / fallback hotkey）
   - fallback hotkey
3. 启动时自动加载设置，托盘菜单状态与设置窗口状态要正确反映当前值。
4. 保存时做异常处理和原子替换，避免写坏配置文件。

十、系统托盘菜单
1. 托盘图标必须常驻。
2. 菜单至少包含：
   - 当前状态（disabled item，例如 `Ready` / `Recording...` / `Refining...`）
   - Language 子菜单
   - LLM Refinement 子菜单
   - Push-to-talk / Hotkey 状态或设置入口
   - Open Settings
   - Quit
3. 菜单交互要即时反映状态变化。

十一、工程交付要求
1. 交付一个完整可构建的解决方案，包括：
   - `.sln`
   - 源码项目
   - 资源文件
   - `README.md`
   - `Makefile`
2. `Makefile` 至少提供这些目标：
   - `build`
   - `run`
   - `publish`
   - `clean`
   - `sign`
3. `publish` 输出自包含的 `win-x64` 发布产物到 `dist/`。
4. `sign` 使用 `signtool` 对可执行文件签名；如果环境变量中没有证书路径和密码，则给出清楚提示并跳过，不要伪造签名成功。
5. README 必须写清楚：
   - 如何构建
   - 如何运行
   - 麦克风权限要求
   - Whisper 模型获取方式
   - Fn 在 Windows 上的限制与 fallback 机制
   - 已知限制
6. 代码中不要遗留“以后再实现”的关键占位。

十二、实现质量要求
1. UI 不要做得像 demo；要克制、精致、干净。
2. 波形必须明显，不要微弱到看不见。
3. 动画要流畅，但不要拖沓。
4. 出错要有降级与提示，不要静默失败。
5. 不要为了省事放弃 partial transcript。
6. 不要为了省事跳过 IME 切换。
7. 不要为了省事把 LLM refine 写成伪接口。
8. 不要为了省事把“Fn 不可用”这个问题忽略掉；必须做真实 fallback。
9. 优先可维护性：服务拆分、接口抽象、清晰命名、少量必要注释。
10. 如果某个底层库在实现中不合适，可以替换，但必须保证最终功能等价或更稳。

十三、建议的模块拆分
请尽量按这种结构实现：
- `App` / 启动与单实例
- `Tray` / 托盘菜单与状态
- `Hotkey` / PTT 触发与全局键监听
- `Audio` / 麦克风采集与 RMS
- `Transcription` / partial + final transcript
- `Overlay` / 悬浮窗 UI 与动画
- `Injection` / 剪贴板、IME、Ctrl+V
- `LLM` / OpenAI-compatible refinement
- `Settings` / 持久化与设置窗口

十四、验收标准
完成后请确保至少满足这些可验证行为：
1. 首次启动后应用进入系统托盘，无主窗口，无任务栏图标。
2. 默认语言为简体中文。
3. 按住当前生效 PTT 键时出现底部胶囊悬浮窗并开始录音。
4. 说话时左侧 5 柱波形随音量明显变化，安静时明显回落。
5. 录音过程中右侧文本实时刷新 partial transcript。
6. 松开按键后完成转录并将文本注入到当前焦点输入框。
7. 当前台是中文/日文/韩文输入法时，仍能可靠粘贴，不被输入法拦截。
8. 启用 LLM refinement 后，松键时先显示 `Refining...`，返回后再注入最终文本。
9. 对“配森”“杰森”这类明显误识别，LLM 能保守修正为 `Python`、`JSON`；而对本来正确的句子不做改写。
10. 重启应用后语言、LLM 开关、API 配置、热键配置都能恢复。

请直接开始实现，生成完整项目代码、配置、README 和 Makefile。不要只输出计划。实现过程中如果你必须做现实约束下的工程权衡，请在 README 中写清楚，但代码本身必须保持可运行。
